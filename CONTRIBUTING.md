# Git の運用規約

このファイルは、本リポジトリにおける Git 履歴の管理規律とコントリビューション規則を定めます。一般的な GitHub の fork や Pull Request の作り方ではなく、本標準リポジトリが要求する厳密なコミット品質と線形履歴の維持手順を扱います。

## 基本理念

本リポジトリの Git 履歴は、単なる作業ログではなく「自立して検証可能な仕様変遷の正本」です。
すべてのコミットは単独で checkout して機械検査を通過しなければならず、中間状態での壊れや手戻りを本流の main に残しません。

## 変更の選別

コミットを作成する前に、作業ツリー内の変更を慎重に選別します。

- 目的の特定: staging の前に git status --short と git diff を精読し、今回のコミットの目的に属する変更だけを特定する。
- 明示的な追加: staging には git add -- <path> または git add -p を使用する。別の目的に属する変更が作業ツリーに混在している状態で、無秩序な git add -A や git add . を使わない。
- 無関係な変更の排除: 今回の目的に属さない既存の変更は、取り消さず、書き換えず、コミットに含めない。
- 記録の分離: docs に置かれた調査、監査、設計の作業記録は、.gitignore に従いリポジトリの追跡対象に含めない。
- 機密情報の遮断: secret、API トークン、秘密鍵、個人情報は決してコミットに含めない。

## コミットの粒度

- 単一の目的と単一の取り消し理由: 一つのコミットには、一つの目的と一つの取り消し理由だけを持たせます。変更ファイル数の少なさではなく、単独で安全に取り消せる論理的境界で分割します。
- アトミック性の確保: 本文の変更、その変更を成立させる検査、および必須の参照更新は、必ず同じコミットに含めます。いずれかを欠いた中間コミットは作ってはなりません。
- 構造改善と振る舞い変更の分離: 振る舞いや規律の変更と、それに直接不要な構造改善は別のコミットに分離します。
- 単独検証可能性: すべてのコミットは、その時点で参照切れや検査失敗を残さず、単独で checkout して verify.sh の全検査を通過できる状態を保ちます。検査ルールを変更した場合は、verify-test.sh に両方向のテストケースを追加します。
- 作業コミットの排除: main ブランチに WIP、fixup!、squash! などの作業途中のコミットを残してはなりません。公開前に amend や interactive rebase を用いて完全に整理と統合を行います。

## コミットメッセージ

件名は scope なし、50文字以内の一行とします。本文および署名やメタデータは記述しません。

```text
<type>: <日本語の要約>
```

### 使用可能な type

| type | 用途 |
|---|---|
| `feat` | 規律、採用、検査機構を追加または更新する |
| `fix` | 誤り、矛盾、検査漏れを是正する |
| `refactor` | 意味や振る舞いを変えずに責務や構造を改善する |
| `docs` | 標準の意味を変えず、運用や解説の文書のみを変更する |
| `test` | 検査やテストケースのみを変更する |
| `build` | ビルド設定や依存関係を変更する |
| `ci` | CI パイプラインの設定を変更する |
| `chore` | 上記に含まれない日常の保守作業 |
| `style` | 意味を変えずにコードや文書の形式を整える |
| `perf` | パフォーマンスを改善する |
| `revert` | 既存のコミットを取り消す |

### 件名の記述ルール

- 目的の記述: 変更したファイル名や編集操作そのものではなく、何のためにその変更を行うのかという目的を具体的に記述する。
- 文体と記号: 複数の語を並べる際は「と」または読点で接続し、中黒は使用しない。
- 帰属表記の禁止: AI 名義の Co-authored-by や、生成ツール名によるクレジット表記は一切付与しない。

#### 望ましいメッセージ例
```text
feat: build の採用判断を再現できるようにする
fix: verifier の異常終了を見逃さない
refactor: skill の変更を独立して取り消せるようにする
docs: Git の誤った履歴操作を防ぐ
```

## コミット前の検証

コミットを作成する前に、staged な差分が一つの目的に閉じていること、および自動検査を通過することを確認します。

```bash
# 1. 差分の確認
git status --short
git diff --staged --check
git diff --staged --stat
git diff --staged

# 2. 自動検査の実行
bash .claude/skills/standard-update/scripts/verify.sh
```

skill を変更した場合は、回帰検査、評価、scoped audit を含む追加手順も省略してはなりません。検査が一度でも失敗した状態、あるいは未検証の項目がある状態でのコミットは厳禁です。

## 履歴の整理とプッシュ

### 線形履歴の原則

- main ブランチは、マージコミットを一切含まない完全な線形履歴を維持します。トピックブランチで作業した場合は、公開前に rebase して一本の線に整えます。
- 公開前のローカルコミットは、目的ごとの境界が明確になるまで amend、rebase、cherry-pick を駆使して自由に再編成できます。

### 公開済み履歴の書き換え規則

公開済みのコミットは原則として書き換えません。ただし、履歴再編が必要となった場合は以下の厳格な手順を踏みます。

1. 事前の承認: 対象範囲と影響を確定し、リポジトリ所有者の明示的な承認を得る。
2. 旧 OID の保全: リモートの旧 OID を記録し、必要に応じてバックアップブランチや bundle を作成する。
3. プッシュ時の安全網: 確認済みの旧 remote OID を明示した --force-with-lease を使用する。無防備な --force は決して使用しない。

```bash
# リモート反映の標準手順
git fetch origin
git push --force-with-lease=refs/heads/main:<old-remote-oid> origin main

# 反映後の整合性確認
git rev-parse HEAD origin/main
```
